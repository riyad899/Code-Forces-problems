#include <iostream>
#include <vector>
#include <set>
#include <algorithm>
using namespace std;

#define ll long long
#define pb push_back
#define endl '\n'
#define FastIO() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

class Solution {
public:
    void sol() {
        int n;
        cin >> n;

    vector<string> s(n);
     vector<string> s2(n);
    for (int i = 0; i < n; i++) {
        cin >> s[i];
        cin >> s2[i];
    }


    }
};
int main() {
}